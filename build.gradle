plugins {
    id 'java-library'
}
repositories {
    mavenCentral()
}

dependencies {
    testImplementation( // ОБЛАСТЬ видимости.
             "com.codeborne:selenide:6.4.0", // selenide СЕЛЕНИД ДОЛЖЕН БЫТЬ ВВЕРХУ!
            "com.github.javafaker:javafaker:1.0.2", // faker для рандома ввода.
            "org.junit.jupiter:junit-jupiter:5.8.2",// Это максимально полный набор Junit( включать нужно такой)
            "org.slf4j:slf4j-simple:1.7.36") // логирование ( чтобы убрать ошибку)
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

test {
    useJUnitPlatform()
}